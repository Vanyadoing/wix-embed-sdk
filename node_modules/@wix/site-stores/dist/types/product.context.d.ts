import type * as productSdkModuleContext from './product.types.d.ts';
type Methods<T> = {
    [P in keyof T as T[P] extends Function ? P : never]: T[P];
};
export declare const productContext: Methods<typeof productSdkModuleContext>;
export {};
