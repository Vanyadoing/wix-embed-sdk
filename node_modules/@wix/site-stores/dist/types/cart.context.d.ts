import type * as cartSdkModuleContext from './cart.types.d.ts';
type Methods<T> = {
    [P in keyof T as T[P] extends Function ? P : never]: T[P];
};
export declare const cartContext: Methods<typeof cartSdkModuleContext>;
export {};
