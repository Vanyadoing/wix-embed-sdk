import type * as navigateSdkModuleContext from './navigate.types.d.ts';
type Methods<T> = {
    [P in keyof T as T[P] extends Function ? P : never]: T[P];
};
export declare const navigateContext: Methods<typeof navigateSdkModuleContext>;
export {};
