import type { Host } from '@wix/site';
import { HostModule, MaybeContext } from '@wix/sdk-types';
import type * as productSdkModuleRuntime from './product.types.d.ts';
type Methods<T> = {
    [P in keyof T as T[P] extends Function ? P : never]: T[P];
};
export declare const productRuntime: MaybeContext<HostModule<Methods<typeof productSdkModuleRuntime>, Host> & Methods<typeof productSdkModuleRuntime>>;
export {};
