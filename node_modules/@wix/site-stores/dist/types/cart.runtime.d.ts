import type { Host } from '@wix/site';
import { HostModule, MaybeContext } from '@wix/sdk-types';
import type * as cartSdkModuleRuntime from './cart.types.d.ts';
type Methods<T> = {
    [P in keyof T as T[P] extends Function ? P : never]: T[P];
};
export declare const cartRuntime: MaybeContext<HostModule<Methods<typeof cartSdkModuleRuntime>, Host> & Methods<typeof cartSdkModuleRuntime>>;
export {};
